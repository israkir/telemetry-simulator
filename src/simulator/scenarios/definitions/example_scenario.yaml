# Reference/template scenario. Excluded from list and mixed workload when using
# sample definitions; run explicitly: otelsim scenario --name example_scenario
# Expected MCP server and tools are defined in the scenario; config supplies key → UUID only.

name: example_scenario
description: Example scenario for documentation and explicit runs.
tags:
  - example

mcp_server: phone
repeat_count: 2
interval_ms: 100
emit_metrics: false
emit_logs: false

data_plane:
  workflow: new_claim
  simulation_goal: happy_path
  control_plane_template: allowed

# Expected MCP server and tools for this scenario (keys only; UUIDs resolved from config).
expected:
  mcp_server: phone
  tools:
    - new_claim

context:
  tenant: toro
  agent: toro-customer-assistant-001
  mcp_server: phone
